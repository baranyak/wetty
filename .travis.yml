language: python

before_install:
 - export DISPLAY=:99.0
 - sh -e /etc/init.d/xvfb start
 - apt-get install netstat
 - apt-get install ifconfig
 - apt-get isntall traceroute

script:
 - "./host_info.sh"

